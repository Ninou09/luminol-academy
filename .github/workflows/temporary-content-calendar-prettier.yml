name: Temporary Content Calendar Prettier
on:
  push:
    branches: [feat/ai-operator-content-calendar]
permissions:
  contents: write
jobs:
  format:
    if: ${{ !contains(github.event.head_commit.message, '[prettier-fix]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: feat/ai-operator-content-calendar
      - uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v4.2.0
        with:
          version: 10.34.5
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v6.0.0
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Apply canonical Prettier formatting
        run: |
          pnpm exec prettier --write \
            apps/admin/app/content-calendar/actions.ts \
            apps/admin/app/content-calendar/page.tsx \
            apps/admin/lib/content-calendar-localization.ts \
            packages/database/src/content-calendar.integration.test.ts \
            packages/database/src/content-calendar.ts \
            packages/validation/src/content-calendar.test.ts \
            packages/validation/src/content-calendar.ts
          git diff --check
      - name: Commit formatting
        run: |
          git config user.name "Luminol CI"
          git config user.email "actions@users.noreply.github.com"
          git add \
            apps/admin/app/content-calendar/actions.ts \
            apps/admin/app/content-calendar/page.tsx \
            apps/admin/lib/content-calendar-localization.ts \
            packages/database/src/content-calendar.integration.test.ts \
            packages/database/src/content-calendar.ts \
            packages/validation/src/content-calendar.test.ts \
            packages/validation/src/content-calendar.ts
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "Format content calendar files [prettier-fix]"
          git push origin HEAD:feat/ai-operator-content-calendar
