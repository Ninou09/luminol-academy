{
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm build",
  "ignoreCommand": "node \"$(git rev-parse --show-toplevel)/scripts/vercel-ignore-build.mjs\""
}
