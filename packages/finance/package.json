{
  "name": "@luminol/finance",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./server": "./src/server.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@luminol/database": "workspace:*",
    "server-only": "^0.0.1",
    "zod": "^4.0.10"
  },
  "devDependencies": {
    "@luminol/config": "workspace:*",
    "eslint": "^10.8.0",
    "typescript": "^7.0.2",
    "vitest": "^3.2.4"
  }
}
