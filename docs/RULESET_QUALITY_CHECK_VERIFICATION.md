# Ruleset quality-check verification

This temporary file exists only to trigger a fresh pull-request CI run so GitHub can expose the repository's `quality` check in the ruleset status-check picker.

It is not intended to be merged into `main`.
